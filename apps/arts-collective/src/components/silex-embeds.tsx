/**
 * The Silex embed renderer (live <eac-embed> React islands) now lives in
 * @elkdonis/silex-render (shared with the dedicated per-org apps). Re-exported
 * here so existing `@/components/silex-embeds` imports keep working.
 */
export { renderSilexHtmlWithEmbeds } from "@elkdonis/silex-render";
